public class CompressedFile extends File{
    byte[] compressedFile;
    public CompressedFile(String name, File original){
        super(name);

        //original lo comprimimos y hacemos algo...

    }
}
